) : (
        <div className="text-center py-8 text-gray-500">
          {searchTerm || statusFilter !== 'all' ? 'No innovation orgs match your search criteria' : 'No innovation submissions found'}
        </div>
      )}
    </div>